import { ProfileExtValidationPipe } from './profile.pipe';

describe('ProfileExtValidationPipe', () => {
  it('should be defined', () => {
    expect(new ProfileExtValidationPipe()).toBeDefined();
  });
});
